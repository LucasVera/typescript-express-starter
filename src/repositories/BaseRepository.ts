import { notImplementedFunction } from "src/util/helper";

class BaseRepository {
  findOne = notImplementedFunction;
  findAll = notImplementedFunction;
  save = notImplementedFunction;
  create = notImplementedFunction;
  update = notImplementedFunction;
  delete = notImplementedFunction;
}

export default BaseRepository;
